{
  "name": "zetapush",
  "version": "0.0.0",
  "description": "Zeta Push Next",
  "main": "index.js",
  "repository": "https://github.com/zetapush/zetapush",
  "author": "ci@zetapush.com",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "husky": "^0.14.3",
    "lerna": "^2.8.0",
    "lint-staged": "^6.1.0",
    "node": "8.9",
    "prettier": "^1.10.2"
  },
  "scripts": {
    "precommit": "lint-staged",
    "prettify": "prettier --write 'packages/*/{bin,src}/**/*.{js,ts}'"
  },
  "lint-staged": {
    "packages/*/{bin,src}/**/*.{js,ts}": [
      "prettier --write",
      "git add"
    ]
  }
}
