{
  "name": "zetapush",
  "description": "Zeta Push Next",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git@github.com:zetapush/zetapush.git"
  },
  "author": "ci@zetapush.com",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "commitizen": "^2.10.1",
    "conventional-changelog-angular": "^5.0.0",
    "conventional-changelog-core": "^3.0.0",
    "conventional-recommended-bump": "^4.0.0",
    "cz-lerna-changelog": "^1.2.1",
    "husky": "^0.14.3",
    "lerna": "^2.11.0",
    "lint-staged": "^7.2.0",
    "prettier": "^1.13.5"
  },
  "scripts": {
    "precommit": "lint-staged",
    "commit": "git-cz",
    "prettify": "prettier --write 'packages/*/{bin,spec,src}/**/*.{js,ts}'",
    "lerna": "lerna",
    "lerna:clean": "lerna clean",
    "lerna:bootstrap": "lerna bootstrap",
    "lerna:build": "lerna run build",
    "lerna:publish": "lerna publish --conventional-commits --changelog-preset angular --force-publish=*",
    "lerna:publish:canary": "lerna publish --canary --conventional-commits --changelog-preset angular --force-publish=*",
    "lerna:test": "lerna run test:ci"
  },
  "lint-staged": {
    "packages/*/{bin,spec,src}/**/*.{js,ts}": [
      "prettier --write",
      "git add"
    ]
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-lerna-changelog"
    }
  }
}
