{
  "name": "@zetapush/example",
  "version": "0.14.0",
  "main": "server/index.js",
  "private": true,
  "scripts": {
    "deploy": "zeta push",
    "register": "zeta register",
    "start": "zeta run",
    "start-skip-provisioning": "zeta run --skip-provisioning"
  },
  "dependencies": {
    "@zetapush/cli": "^0.14.0",
    "@zetapush/platform": "^0.14.0"
  }
}