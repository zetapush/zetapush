{
  "name": "@zetapush/example",
  "main": "worker/index.ts",
  "private": true,
  "scripts": {
    "deploy": "zeta push",
    "register": "zeta register",
    "prestart": "zeta --version",
    "start": "zeta run -vvv",
    "help": "zeta run --help",
    "troubleshoot": "zeta troubleshoot"
  },
  "dependencies": {
    "@zetapush/cli": "*",
    "@zetapush/platform": "*",
    "typescript": "^2.9.1"
  }
}
