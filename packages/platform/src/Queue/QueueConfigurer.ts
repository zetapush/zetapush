import { Configurer } from '../Core';
import { Queue } from './Queue';

/**Producer consumer service. Users can submit tasks and other users consume them*/
export class QueueConfigurer extends Configurer {}
